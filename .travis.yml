language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-sass-coffee-ember' ]; then cd .. && eval "mv $currentfolder generator-sass-coffee-ember" && cd generator-sass-coffee-ember; fi

